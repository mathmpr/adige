## Test Structure

This directory contains automated tests and fixtures for the Adige core.

- `Fixtures/web/controllers`: controllers used to exercise router auto-discovery
- future `unit/`: isolated tests for core classes
- future `integration/`: small end-to-end framework flows
