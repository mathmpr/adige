# STABILIZATION 0.0.5: Views, Migrations and Validators

This document defines the `0.0.5` stabilization line, focused on the new view contract, the first migration workflow, and the boundary of validator work around this release.

Scope of this stabilization:
- stabilize a small synchronous view layer integrated with the current controller/app flow
- define the first public migration contract around file discovery, execution and metadata tracking
- keep bootstrap configuration explicit for view and migration paths
- preserve the stabilized contracts from `0.0.1` through `0.0.4`

Out of scope for now:
- a full template engine beyond PHP view files
- layouts, sections or asset pipelines as part of the stable contract
- advanced migration operations such as rename table, rename column or cross-database schema diffing
- a full validator subsystem as a closed public API in this release

## Current Summary

Estimated overall state:
- approximate progress: `views and migrations completed for 0.0.5`
- validator work remains outside the closed public contract of this line

What changed materially:
- a reusable view component now exists in [`src/core/BaseView.php`](/home/mathmpr/PhpstormProjects/adige/src/core/BaseView.php)
- controllers can now render views through [`src/core/controller/BaseController.php`](/home/mathmpr/PhpstormProjects/adige/src/core/controller/BaseController.php)
- the app container now recognizes a dedicated view handler through [`src/core/Adige.php`](/home/mathmpr/PhpstormProjects/adige/src/core/Adige.php) and [`src/core/App.php`](/home/mathmpr/PhpstormProjects/adige/src/core/App.php)
- the first migration abstractions now exist in:
  - [`src/core/database/Migration.php`](/home/mathmpr/PhpstormProjects/adige/src/core/database/Migration.php)
  - [`src/core/database/MigrationDialect.php`](/home/mathmpr/PhpstormProjects/adige/src/core/database/MigrationDialect.php)
  - [`src/core/database/MigrationField.php`](/home/mathmpr/PhpstormProjects/adige/src/core/database/MigrationField.php)
- migration SQL compilation now has explicit MySQL and SQLite implementations in:
  - [`src/core/database/dialects/mysql/MysqlMigration.php`](/home/mathmpr/PhpstormProjects/adige/src/core/database/dialects/mysql/MysqlMigration.php)
  - [`src/core/database/dialects/sqlite/SqliteMigration.php`](/home/mathmpr/PhpstormProjects/adige/src/core/database/dialects/sqlite/SqliteMigration.php)
- the console now has a first migration controller in [`src/console/controllers/MigrateController.php`](/home/mathmpr/PhpstormProjects/adige/src/console/controllers/MigrateController.php)
- contract coverage now exists in:
  - [`tests/Unit/Core/BaseViewContractTest.php`](/home/mathmpr/PhpstormProjects/adige/tests/Unit/Core/BaseViewContractTest.php)
  - [`tests/Unit/Database/MigrationContractTest.php`](/home/mathmpr/PhpstormProjects/adige/tests/Unit/Database/MigrationContractTest.php)
  - [`tests/Unit/Console/MigrateControllerTest.php`](/home/mathmpr/PhpstormProjects/adige/tests/Unit/Console/MigrateControllerTest.php)

Architectural direction captured in this line:
- views are a small container-managed capability, not a global filesystem convention
- migrations are discovered by file and identified by filename, not by class name
- migration metadata is part of the framework contract through a `migrations` table with `batch` tracking
- bootstrap configuration remains explicit and app-controlled

---

## Phase 0: Freeze and diagnose the new extension surfaces

Goal:
- define the smallest stable contracts for views and migrations before expanding them

Current status:
- completed

Tasks:
- [x] define a baseline view component contract
- [x] decide how controllers render views
- [x] decide how migrations are discovered and named
- [x] decide where migration path configuration lives
- [x] decide whether validator work belongs to the closed `0.0.5` public API

Notes:
- views and migrations were introduced after the request/response and ORM contracts were already stabilized
- the chosen direction avoids hardcoding project-specific roots in the public API
- validator work was intentionally not promoted to a stable public subsystem in this line

Acceptance criteria:
- the new surfaces are small enough to document and test as contracts

---

## Phase 1: Stabilize the view contract

Goal:
- provide a minimal rendering primitive applications can rely on

Current status:
- completed

Tasks:
- [x] add a dedicated `BaseView` component
- [x] make the view directory instance-based instead of global state
- [x] allow explicit alias registration such as `@shared/partial`
- [x] prevent path traversal and absolute-path rendering
- [x] ensure output buffers are cleaned correctly on render failure
- [x] expose a small escaping helper
- [x] expose render lifecycle hooks

Notes:
- `BaseView` now exposes:
  - `render(string $view, array $params = []): string`
  - `escape(string|int|float|null $value): string`
  - `setViewDirectory(string $path): static`
  - `getViewDirectory(): string`
  - `registerAlias(string $alias, string $directory): static`
  - `getAliases(): array`
- `BaseView` now emits:
  - `EVENT_BEFORE_RENDER`
  - `EVENT_AFTER_RENDER`
  - `EVENT_RENDER_ERROR`
- `BaseController::render()` now delegates to the configured app view handler
- `Adige::VIEW_HANDLER` is now part of the container-facing handler keys

Acceptance criteria:
- controllers can render deterministic PHP view files without leaking state between renders

---

## Phase 2: Stabilize the first migration contract

Goal:
- provide a predictable schema-change workflow aligned with the current ORM architecture

Current status:
- completed

Tasks:
- [x] add a public `Migration` base abstraction
- [x] add a fluent `MigrationField` builder
- [x] add explicit dialect compilation for MySQL and SQLite
- [x] support `createTable`, `dropTable`, `addColumn` and `dropField`
- [x] guarantee the metadata table exists before migration execution
- [x] make migration discovery depend on filename instead of class name
- [x] allow anonymous-class migrations returned by file

Notes:
- migration files now follow the stabilized format:

```php
<?php

use Adige\core\database\Migration;

return new class extends Migration {
    public function up(): void
    {
    }

    public function down(): void
    {
    }
};
```

- `Migration` now supports connection injection after construction through `setConnection()`
- migration metadata lives in the `migrations` table with:
  - `id`
  - `name`
  - `batch`
  - `created_at`
- metadata creation/upgrade now also ensures the `batch` column exists for older metadata tables

Acceptance criteria:
- migration execution is deterministic across supported drivers and tied to a small documented contract

---

## Phase 3: Stabilize the console migration workflow

Goal:
- expose migrations through the existing controller/action console model

Current status:
- completed

Tasks:
- [x] add `migrate/create`
- [x] add `migrate/up`
- [x] add `migrate/down`
- [x] support app-configurable migration directory
- [x] track applied migrations by batch
- [x] make `down` revert whole batches instead of single files by default

Notes:
- `MigrateController` now exposes:
  - `actionCreate(string $name): string`
  - `actionUp(): string`
  - `actionDown(?int $steps = null): string`
- `actionUp()` applies all pending migrations in one new batch
- `actionDown()` reverts the latest batch when `steps` is omitted
- `actionDown(2)` reverts the two latest batches, and so on
- migration discovery uses the configured filesystem path and stores the filename as the canonical migration name

Acceptance criteria:
- migrations can be created, applied and reverted through the stabilized console flow without a second command system

---

## Phase 4: Bootstrap configuration and project boundary cleanup

Goal:
- keep view and migration configuration explicit without coupling them to `/app`

Current status:
- completed

Tasks:
- [x] keep view configuration container-driven
- [x] allow migration path configuration through bootstrap/app container config
- [x] preserve a sensible default migration directory at `ROOT/migrations`
- [x] keep the framework core independent from a specific playground app structure

Notes:
- `BaseView` is configured through `Adige::VIEW_HANDLER`
- migrations are configured through `Adige::MIGRATIONS_CONFIG`
- the default migration path is `ROOT/migrations` when no app override exists
- this direction supports the future removal of `/app` as a required project boundary

Acceptance criteria:
- view and migration configuration is explicit, app-controlled and not hardwired to the playground layout

---

## Phase 5: Coverage and operational fixes

Goal:
- ensure the new public contracts remain refactor-safe

Current status:
- completed

Tasks:
- [x] cover view aliasing, nested rendering and path hardening
- [x] cover migration SQL compilation and transaction flow
- [x] cover console migration batch semantics
- [x] fix connection transaction-state drift after DDL in supported drivers

Notes:
- the `Connection` contract now synchronizes internal transaction state with the real PDO transaction state before `commit` and `rollback`
- this avoids false active-transaction assumptions after DDL that may auto-close transactions on some drivers such as MySQL

Acceptance criteria:
- the new contracts have focused regression tests and avoid the main operational pitfalls found during implementation

---

## Short Public Contract

### Views

- `BaseView` is the stable base view component
- controller rendering uses `BaseController::render()`
- view resolution supports:
  - a configured base directory
  - explicit aliases such as `@shared/partial`
- view names reject path traversal and absolute paths
- render hooks are part of the public lifecycle contract

### Migrations

- migration identity is the filename
- a migration file must return a `Migration` instance
- the migration object may receive its `Connection` after construction
- `migrate/up` applies all pending migrations in one batch
- `migrate/down` reverts the latest batch, or the latest `N` batches when `steps` is provided
- metadata is tracked in the `migrations` table through `name`, `batch` and `created_at`

### Validators

- validator work is not yet documented as a stable public subsystem in `0.0.5`
- applications should not treat any in-progress validator-related experimentation as compatibility-protected
